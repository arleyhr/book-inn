terraform {
  backend "azurerm" {
    resource_group_name  = "terraform-state-rg"
    storage_account_name = "tfstate<STORAGE_ACCOUNT_SUFFIX>"
    container_name       = "tfstate"
    key                 = "terraform.tfstate"
  }
}
